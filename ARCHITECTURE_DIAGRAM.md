# 🏗️ FastFoodie Domain Architecture

## Current Setup (Before Domain)

```
┌─────────────────┐
│  Mobile Apps    │
│  (Customer,     │
│   Restaurant,   │
│   Delivery,     │
│   Admin)        │
└────────┬────────┘
         │
         │ HTTP Request
         │ http://52.22.224.42:8000
         ↓
┌─────────────────────────────────┐
│      EC2 Instance               │
│  ┌──────────────────────────┐   │
│  │  FastAPI Backend         │   │
│  │  (Port 8000)             │   │
│  │  - REST APIs             │   │
│  │  - Socket.IO             │   │
│  └──────────────────────────┘   │
│  ┌──────────────────────────┐   │
│  │  MySQL Database          │   │
│  │  (Docker Container)      │   │
│  └──────────────────────────┘   │
└─────────────────────────────────┘
```

**Issues:**
- ❌ No SSL/HTTPS (insecure)
- ❌ IP address exposed
- ❌ Hard to remember URL
- ❌ Can't use in production

---

## New Setup (With Domain + Nginx)

```
┌─────────────────┐
│  Mobile Apps    │
│  (Customer,     │
│   Restaurant,   │
│   Delivery,     │
│   Admin)        │
└────────┬────────┘
         │
         │ HTTPS Request
         │ https://dharaidelivery.online
         ↓
┌──────────────────────────────────────────────┐
│         Internet (Port 443)                  │
└──────────────────┬───────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────┐
│              EC2 Instance                           │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │          Nginx (Reverse Proxy)               │  │
│  │  ┌────────────────────────────────────────┐  │  │
│  │  │  Port 443 (HTTPS)                      │  │  │
│  │  │  - SSL Termination                     │  │  │
│  │  │  - Security Headers                    │  │  │
│  │  │  - Request Routing                     │  │  │
│  │  └────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────┐  │  │
│  │  │  Port 80 (HTTP)                        │  │  │
│  │  │  - Redirects to HTTPS                  │  │  │
│  │  └────────────────────────────────────────┘  │  │
│  └──────────────┬───────────────────────────────┘  │
│                 │                                   │
│                 │ Proxy Pass (localhost:8000)       │
│                 ↓                                   │
│  ┌──────────────────────────────────────────────┐  │
│  │      FastAPI Backend (Port 8000)             │  │
│  │  ┌────────────────────────────────────────┐  │  │
│  │  │  REST API Endpoints                    │  │  │
│  │  │  - /auth/*                             │  │  │
│  │  │  - /restaurants/*                      │  │  │
│  │  │  - /orders/*                           │  │  │
│  │  │  - /customer/*                         │  │  │
│  │  │  - /delivery-partner/*                 │  │  │
│  │  └────────────────────────────────────────┘  │  │
│  │  ┌────────────────────────────────────────┐  │  │
│  │  │  Socket.IO (WebSocket)                 │  │  │
│  │  │  - /socket.io/*                        │  │  │
│  │  │  - Real-time order updates             │  │  │
│  │  └────────────────────────────────────────┘  │  │
│  └──────────────┬───────────────────────────────┘  │
│                 │                                   │
│                 ↓                                   │
│  ┌──────────────────────────────────────────────┐  │
│  │      MySQL Database                          │  │
│  │      (Docker Container)                      │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

**Benefits:**
- ✅ HTTPS/SSL encryption
- ✅ Professional domain name
- ✅ Security headers
- ✅ WebSocket support
- ✅ Production-ready

---

## Request Flow

### 1. Regular API Request

```
Mobile App
    │
    │ GET https://dharaidelivery.online/restaurants/nearby
    ↓
Nginx (Port 443)
    │
    │ 1. SSL Termination
    │ 2. Add security headers
    │ 3. Proxy to backend
    ↓
FastAPI (Port 8000)
    │
    │ 1. Authenticate
    │ 2. Query database
    │ 3. Return JSON
    ↓
MySQL Database
    │
    │ Return restaurant data
    ↓
FastAPI
    │
    │ Format response
    ↓
Nginx
    │
    │ Add headers, send response
    ↓
Mobile App
```

---

### 2. WebSocket Connection (Real-time Updates)

```
Mobile App
    │
    │ Connect wss://dharaidelivery.online/socket.io/
    ↓
Nginx (Port 443)
    │
    │ 1. Detect WebSocket upgrade request
    │ 2. Set Connection: upgrade headers
    │ 3. Proxy to backend
    ↓
FastAPI Socket.IO (Port 8000)
    │
    │ 1. Establish WebSocket connection
    │ 2. Keep connection alive
    │ 3. Send real-time order updates
    ↓
Mobile App
    │
    │ Receive order status updates
    │ Auto-refresh UI
```

---

### 3. Image Upload

```
Mobile App
    │
    │ POST https://dharaidelivery.online/restaurants/images
    │ Content-Type: multipart/form-data
    ↓
Nginx (Port 443)
    │
    │ 1. Check file size (max 20MB)
    │ 2. Proxy to backend
    ↓
FastAPI (Port 8000)
    │
    │ 1. Validate image
    │ 2. Upload to S3/local storage
    │ 3. Save URL in database
    ↓
MySQL Database
    │
    │ Store image URL
    ↓
FastAPI
    │
    │ Return image URL
    ↓
Mobile App
```

---

## DNS Configuration

```
┌─────────────────────────────────────────┐
│  Domain Registrar (GoDaddy/Namecheap)  │
│                                         │
│  dharaidelivery.online                  │
│                                         │
│  DNS Records:                           │
│  ┌───────────────────────────────────┐  │
│  │ Type: A                           │  │
│  │ Name: @                           │  │
│  │ Value: 52.22.224.42 (EC2 IP)     │  │
│  │ TTL: 300                          │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ Type: A                           │  │
│  │ Name: www                         │  │
│  │ Value: 52.22.224.42 (EC2 IP)     │  │
│  │ TTL: 300                          │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
                   │
                   │ DNS Resolution
                   ↓
┌─────────────────────────────────────────┐
│         EC2 Instance                    │
│         IP: 52.22.224.42                │
└─────────────────────────────────────────┘
```

---

## SSL Certificate Flow

```
┌─────────────────┐
│  Let's Encrypt  │
│  (Free SSL)     │
└────────┬────────┘
         │
         │ 1. Request certificate
         │    for dharaidelivery.online
         ↓
┌─────────────────────────────────┐
│  Certbot (on EC2)               │
│                                 │
│  1. Generate CSR                │
│  2. Prove domain ownership      │
│     (HTTP-01 challenge)         │
│  3. Receive certificate         │
└────────┬────────────────────────┘
         │
         │ 2. Install certificate
         ↓
┌─────────────────────────────────┐
│  Nginx                          │
│                                 │
│  /etc/letsencrypt/live/         │
│    dharaidelivery.online/       │
│    ├── fullchain.pem            │
│    └── privkey.pem              │
└─────────────────────────────────┘
         │
         │ 3. Serve HTTPS
         ↓
┌─────────────────┐
│  Mobile Apps    │
│  🔒 Secure!     │
└─────────────────┘
```

**Certificate Auto-Renewal:**
```
Every 12 hours (cron job)
    │
    ↓
Certbot checks expiry
    │
    ├─→ < 30 days left? → Renew certificate
    │                      │
    │                      ↓
    │                   Reload Nginx
    │
    └─→ > 30 days left? → Do nothing
```

---

## Security Layers

```
┌─────────────────────────────────────────────────┐
│  Layer 1: AWS Security Group                   │
│  - Only allow ports 80, 443, 22                │
│  - Block direct access to port 8000            │
└────────────────┬────────────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────────────┐
│  Layer 2: Nginx                                │
│  - SSL/TLS encryption                          │
│  - Security headers (HSTS, X-Frame-Options)    │
│  - Rate limiting (optional)                    │
│  - Request size limits (20MB)                  │
└────────────────┬────────────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────────────┐
│  Layer 3: FastAPI                              │
│  - JWT authentication                          │
│  - Input validation                            │
│  - SQL injection prevention (SQLAlchemy)       │
│  - CORS configuration                          │
└────────────────┬────────────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────────────┐
│  Layer 4: Database                             │
│  - Only accessible from localhost              │
│  - User permissions                            │
│  - Encrypted connections                       │
└─────────────────────────────────────────────────┘
```

---

## File Structure on EC2

```
/home/ec2-user/
├── fastfoodie-backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── routers/
│   │   └── models/
│   ├── docker-compose.yml
│   ├── .env
│   └── uploads/
│
/etc/nginx/
├── nginx.conf
├── sites-available/
│   └── dharaidelivery
└── sites-enabled/
    └── dharaidelivery → ../sites-available/dharaidelivery

/etc/letsencrypt/
├── live/
│   └── dharaidelivery.online/
│       ├── fullchain.pem
│       └── privkey.pem
└── renewal/
    └── dharaidelivery.online.conf

/var/log/nginx/
├── dharaidelivery-access.log
└── dharaidelivery-error.log
```

---

## Port Mapping

| Port | Service | Access | Purpose |
|------|---------|--------|---------|
| 22 | SSH | Your IP only | Server management |
| 80 | Nginx HTTP | Public | Redirect to HTTPS |
| 443 | Nginx HTTPS | Public | Main entry point |
| 8000 | FastAPI | Localhost only | Backend API |
| 3306 | MySQL | Docker network | Database |

**Important:** Port 8000 should NOT be accessible from the internet!

---

## Monitoring Dashboard (Conceptual)

```
┌─────────────────────────────────────────────────┐
│  Nginx Access Logs                             │
│  /var/log/nginx/dharaidelivery-access.log      │
│                                                 │
│  52.1.2.3 - [13/Feb/2026:12:00:00] "GET /docs" │
│  200 1234 "Mozilla/5.0..."                     │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  Backend Logs                                  │
│  docker logs fastfoodie_api                    │
│                                                 │
│  INFO: Order #123 created                      │
│  INFO: WebSocket connected: customer_456       │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  SSL Certificate Status                        │
│  certbot certificates                          │
│                                                 │
│  Certificate Name: dharaidelivery.online       │
│  Expiry Date: 2026-05-14                       │
│  Valid: ✅                                      │
└─────────────────────────────────────────────────┘
```

---

## Summary

**Before:** `http://52.22.224.42:8000` ❌
- No encryption
- IP-based
- Not production-ready

**After:** `https://dharaidelivery.online` ✅
- SSL/HTTPS encryption
- Professional domain
- Production-ready
- WebSocket support
- Security headers
- Auto-renewing SSL

**Your apps will connect to:** `https://dharaidelivery.online`
